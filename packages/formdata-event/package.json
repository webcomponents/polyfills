{
  "name": "@webcomponents/formdata-event",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc && gulp",
    "build:watch": "chokidar --initial 'ts_src/**/*.ts' -c 'npm run build'",
    "prepack": "npm run build"
  },
  "files": [
    "formdata-event.min.js*",
    "src/"
  ],
  "devDependencies": {
    "typescript": "^3.9.3"
  }
}
